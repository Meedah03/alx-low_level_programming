#include "main.h"

/**
 * Function: times_table - Prints 9 Times_Table
 *
 * Return: 0
 */

void times_table(void)
{
	int a;
       int b;

	for (a = 0; a < 10; a++)
	{
		for (b = 0; b < 10; b++)
		{
			printf("%d", i * j);
			if (j < 9)
			{
				printf(", ");
			}
		}
		printf("\n");
	}
	return (0)
}
