#include "main.h"
#include <string.h>
/**
 * _strcat - concatenates s1 and s2
 * @dest: S1
 * @src: S2
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	return (dest);
}
