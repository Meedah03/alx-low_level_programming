#include "main.h"
#include <string.h>
/**
 * _strlen - length of string
 * @s: string
 *
 * Return: The length of string
 */

int _strlen(char *s)
{
	int i;
	

	i = strlen(const char *s);
	s[i] != '\0';
	i++
	return (i);
}
