#include "main.h"

/**
 * puts_half - prints half
 * @str: string
 * Return 0
 */
void puts_half(char *str)
{
	int i;
	int l;
	
	i = 0
	l = i / 2;
	for (i = l; str[i] >= l; i++)
			_putchar(str[i]);
}
