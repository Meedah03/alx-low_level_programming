#include "main.h"

/**
 * puts_half - prints half
 * @str: string
 * Return 0
 */
void puts_half(char *str)
{
	char B[i];
	int i;
	int l;
	int m;

	l = strlen(str[i]);
	m = (l / 2);
		for (i = m; B = 0 && i <= l; i++, B++)
			_putchar(B[i]);
}
