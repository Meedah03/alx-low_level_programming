#include "main.h"
#include <string.h>
/**
 * *_strcpy - copy string
 * @dest: string
 * @src: string
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{

	strcpy(dest, src);

	return (dest);
}
