#include <stdio.h>

/**
 * Description: main - print numbers
 * Return: Always 0 (Success)
 */

int main(void)
{
	char W;

	W = 0;
	while (W < 9)
	{
		putchar(W);
	W++;
	}
	putchar('\n');

	return (0);
}
