#include <stdio.h>

/**
 * Description: main - print single digits
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a = 0;

	while (a <= 9)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");

	return (0);
}
